# Verification — September 15, 2026
- Eleven workflow tests passed: private draft exclusion and public links; approval revision binding; source-window validation and HTML escaping; default date; feature dependency rollback; scoped Git commit/push to a local test remote; Kit create/update/retry and sent-draft protection; definite versus uncertain API failures; agent output ingestion/no overwrite; local editor save and stale-version handling.
- Review JavaScript syntax checked. Skill validator passed. Static build succeeded.
- A real signed-in Codex CLI run with live web research completed and produced a five-story draft in .newsroom/live-check-tnpc62ep/. This exercised research, structured output, validation and private notes without changing the prepared edition.
- This confirms workflow execution, not factual accuracy. The live draft included a journal homepage where a direct paper link was needed; editorial instructions now explicitly prohibit that substitution. Human source review remains necessary.
- AWS deployment and Kit API integration were not exercised against real accounts because domain, repository and account settings are not configured.

## Agent-owned pipeline update
Fifteen tests passed, including revision-bound evidence checks, feedback approval blocking, private review packets, and immutable release package retries. The weekly Tuesday 09:00 thread automation is active. Skill validation passed. Existing edition is flagged for editorial revision; it has not been approved. Live publishing and newsletter sending remain deferred.
